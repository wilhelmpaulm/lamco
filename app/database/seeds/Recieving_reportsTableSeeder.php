<?php

class Recieving_reportsTableSeeder extends Seeder {

    public function run()
    {
    	// Uncomment the below to wipe the table clean before populating
    	// DB::table('recieving_reports')->delete();

        $recieving_reports = array(

        );

        // Uncomment the below to run the seeder
        // DB::table('recieving_reports')->insert($recieving_reports);
    }

}